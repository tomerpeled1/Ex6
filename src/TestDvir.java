public class TestDvir {
    public static void main(String[] args) {
        String expression = "a || f && 3";
        String s = "(3 || 5)";
        int expressionStart = s.indexOf('(');
        int expressionEnd = s.indexOf(')');

    }
}
